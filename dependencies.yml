version: 2
dependencies:
- type: php
  path: /
  settings:
    # options for composer install/update/require
    composer_options: "--ignore-platform-reqs --no-scripts"
    
    # automatically close outdated open PRs (works with GitHub only)
    related_pr_behavior: close
