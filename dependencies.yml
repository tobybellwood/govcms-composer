version: 2
dependencies: 
- type: php
    path: /
    manifest_updates: 
      filters: 
        - name: ".*"
          versions: "Y.Y.Y"
    settings: 
      composer_options: "--ignore-platform-reqs --no-scripts"
      github_labels: 
        - dependencies
    
